# ── Secrets ──
.env

# ── Data (too large for git) ──
data/

# ── Trained models ──
models/

# ── Reports / plots ──
reports/

# ── Lit-review PDFs (large + copyrighted; /research skill cache) ──

# ── Python ──
__pycache__/
*.pyc
*.pyo
*.egg-info/
dist/
build/

# ── Jupyter ──
.ipynb_checkpoints/

# ── IDE ──
.vscode/
.idea/

# claude-code framework
.claude/settings.local.json
.claude/cmd-log.txt
.claude/aggressive-*.log
.claude/aggressive-*.plan.txt
.claude/skills/
.claude/.nfs*
.claude/scheduled_tasks.lock
HANDOFF.md
_artifacts/
.olauth

# generated outputs (never tracked; these flood VSCode otherwise)
experiments/
logs/
data/
results/
*.pt
*.pth
.smoke/

# claude-code framework
.claude/settings.local.json
.claude/cmd-log.txt
.claude/aggressive-*.log
.claude/aggressive-*.plan.txt
.claude/skills/
.claude/worktrees/
.claude/.nfs*
.claude/scheduled_tasks.lock
HANDOFF.md
_artifacts/
.olauth

# heavy / generated outputs — never tracked (flood git + VSCode).
# results/ is TRACKED (markdown report + figures progress surface).
/experiments/
/logs/
/data/
/tracks/*/logs/
/tracks/*/experiments/
/literature/**
!/literature/manifest.json
*.pt
*.pth
*.pkl
.smoke/
